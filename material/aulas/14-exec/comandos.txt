comandos interessantes:

>>> man <funcao> : retorna a documentacao da funcao
ex: man atof

>>> gcc -Wall arquivo.c -o arquivo 

# a chamada exec é usada para carregar programas na memória e executá-las
exemplo:  execvp(prog, args);

